import styled from 'styled-components'

export const SimpleUploadWrapper = styled.div`
  position: relative;
  /** 切换上传和input */
  .switch {
    position: absolute;
    right: 5px;
    top: -27px;
    width: 50px;
    white-space: nowrap;
    overflow: hidden;
  }
`
